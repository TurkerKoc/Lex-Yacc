Enter your BNF rules into input.txt
(Your rules should be in this form: <A> -> <B> a <s> c) 
you should put whitespace before and after assign(->) and before and after each nonterminal and terminal on the right hand side. (A whitespace can contain a mixture of space and tab characters.
You should write each assign statement into new line.

open terminal 

by using cd command go to the directory of this file
(Ex: "cd /Users/turker/Desktop/PL-term-project")

write "make" 

write "./project input.txt"

now your output is in output.txt
you can write "cat output.txt" to see your output